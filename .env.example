GOOGLE_API_KEY=              # 필수 — aistudio.google.com 에서 발급
ELEVENLABS_API_KEY=          # 필수 — elevenlabs.io 에서 발급 (없으면 macOS say 폴백)

# 선택
USER_NAME=                   # JARVIS가 이름으로 호칭 (예: Tony)
ELEVENLABS_VOICE_ID=JBFqnCBsd6RMkjVDRZzb   # George (British)
GEMINI_MODEL=gemini-2.0-flash
JARVIS_AMBIENT_ENABLED=false
JARVIS_AMBIENT_DIR=
